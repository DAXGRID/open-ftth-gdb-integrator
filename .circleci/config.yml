version: 2.1

executors:
  dotnet-core-sdk:
    docker:
    - image: mcr.microsoft.com/dotnet/core/sdk:3.1
  docker-publisher:
    environment:
      IMAGE_NAME: openftth/gdb-integrator
    docker:
      - image: circleci/buildpack-deps:stretch

jobs:
  build-app:
    executor: dotnet-core-sdk
    steps:
      - checkout
      - run:
          name: Apt update
          command: apt-get update;
      - run:
          name: Install taskfile
          command: curl -sL https://taskfile.dev/install.sh | sh
      - run:
          name: Export taskrunner
          command: |
            echo 'export PATH=$HOME/bin:$PATH' >> $BASH_ENV
            source /home/circleci/.bashrc
      - run:
          name: Restore
          command: task restore
      - run:
          name: Build
          command: task build-release

  test-app:
    executor: dotnet-core-sdk
    steps:
      - checkout
      - run:
          name: Apt update
          command: apt-get update;
      - run:
          name: Install taskrunner
          command: curl -sL https://taskfile.dev/install.sh | sh
      - run:
          name: Export taskrunner
          command: |
            echo 'export PATH=$HOME/bin:$PATH' >> $BASH_ENV
            source /home/circleci/.bashrc
      - run:
          name: Test
          command: task test-verbose

  build-docker-image:
    executor: docker-publisher
    steps:
     - checkout
     - setup_remote_docker
     - run:
         name: Build Docker image
         command: |
            docker build -t $IMAGE_NAME:latest .
     - run:
         name: Archive Docker image
         command: docker save -o image.tar $IMAGE_NAME
     - persist_to_workspace:
         root: .
         paths:
          - ./image.tar

  docker-publish-image:
    executor: docker-publisher
    steps:
     - attach_workspace:
         at: /tmp/workspace
     - setup_remote_docker
     - run:
         name: Load archived Docker image
         command: docker load -i /tmp/workspace/image.tar
     - run:
         name: Publish Docker Image to Docker Hub
         command: |
           echo "$GDB_INTEGRATOR_DOCKER_PASSWORD" | docker login -u "$GDB_INTEGRATOR_DOCKER_LOGIN" --password-stdin
           IMAGE_TAG=$(CIRCLE_TAG/v/'')
           docker tag $IMAGE_NAME:latest $IMAGE_NAME:$IMAGE_TAG
           docker push $IMAGE_NAME:latest
           docker push $IMAGE_NAME:$IMAGE_TAG

workflows:
  build-test-upload_image:
    jobs:
      - build-app
      - test-app:
          requires:
            - build-app
      - build-docker-image:
          requires:
            - test-app
          filters:
            tags:
              only: /^v.*/
            branches:
              ignore: /.*/
      - docker-publish-image:
          requires:
           - build-docker-image
          filters:
            tags:
              only: /^v.*/
            branches:
              ignore: /.*/
